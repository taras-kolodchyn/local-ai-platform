# Explicit opt-in only. Use a dedicated fine-grained, read-only GitHub token.
# Keep the "Bearer " prefix because LiteLLM forwards this complete header value.
GITHUB_MCP_AUTHORIZATION="Bearer REPLACE_WITH_SCOPED_READ_ONLY_TOKEN"
