# Hermes comparison exercise

Review the same Rust reports service through `local-qwen`. Use the read-only retrieval tool to locate the internal retry and container rules. Report the SQL-injection, missing-authentication, Dockerfile, and Helm findings, then implement one bounded fix with a regression test and show the final diff. Do not access external services or unrelated host paths.
